\documentclass[twocolumn]{article}

\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{xcolor}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{subcaption}
\usepackage{natbib}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\bibliographystyle{plainnat}

\title{PARSE: Principled Architecture Retention through Scenario-Embedded Pruning for Fine-Grained Capability-Preserving Language Model Compression}

\author{Anonymous for Review}

\date{}

\begin{document}

\maketitle

\begin{abstract}
The prevailing paradigm for efficient LLM deployment oscillates between two unsatisfactory extremes: uniform structural pruning, which blindly compresses models without regard for the heterogeneous distribution of capabilities within Transformer layers; and task-specific architecture design, which builds narrow expert models that abandon broad pretraining knowledge.
This work introduces a third paradigm: treating model compression as a capability-preserving surgical procedure operating at the intersection of language, discipline, and scenario dimensions.
PARSE (Principled Architecture Retention through Scenario-Embedded Pruning) introduces a tri-axial Capability Importance Tensor (CIT) that independently quantifies each layer's contribution along three conceptually distinct axes: Language, Discipline, and Scenario.
Activation-based CIT probing on Qwen3.5-0.8B and Qwen3-0.6B across 12 preservation profiles reveals: (1) strong depth-dependent concentration ($\bar{r} = 0.9945$ on Qwen3.5-0.8B, $\bar{r} = 0.9927$ on Qwen3-0.6B) with all 12 profiles converging to identical layer importance ranking---confirming that tri-axial decomposition collapses to depth-weighted ranking under high inter-axis correlation; (2) this concentration pattern is cross-architecture reproducible, confirmed across Mamba-hybrid and standard Transformer architectures; (3) real pipeline validation on Qwen3-0.6B shows that FFN removal without functional flywheel recovery causes catastrophic collapse (GSM8K CRR = 0.0, PPL = $\infty$), while the projected CRR $\geq$ 0.91 values remain unvalidated computed estimates; (4) the current flywheel recovery mechanism fails to converge (0 successful training steps), requiring redesign of NoFFN block initialization.
PARSE identifies capability-critical layers via CIT and proposes surgical FFN transplantation with Dynamic Capability Routing (DCR, 0.08M parameters), but the compression pipeline itself requires further engineering work before achieving its intended capability-preserving compression.
\end{abstract}

\textbf{Keywords:} Capability-Aware Model Compression $\bullet$ Fine-Grained Pruning $\bullet$ Architecture Transplantation $\bullet$ Language-Discipline-Scenario Tri-Axis Analysis $\bullet$ Dynamic Routing $\bullet$ Tiny Language Models

\section{Introduction}

\subsection{The ``One-Size-Fits-All'' Fallacy in Model Compression}

The rapid scaling of Large Language Models has produced systems with remarkable breadth---from multilingual translation to mathematical theorem proving---all contained within a single parameter set.
However, this universality comes at a steep deployment cost.
A Qwen3.5-0.8B model, modest by current standards, still requires approximately 1.5\,GB of VRAM and operates at 2--15 tok/s on mid-range consumer GPUs, effectively excluding it from real-time edge applications.

The prevailing response has bifurcated into two camps.
The \textbf{compression camp} applies uniform sparsity constraints---LLM-Pruner \citep{ma2023llmpruner} uses gradient-based coupling, SparseGPT \citep{frantar2023sparsegpt} achieves one-shot 50\% sparsity through second-order optimization, Wanda \citep{sun2023wanda} computes weight-activation product scores, and oBERT \citep{kurtic2022obert} pushes compression to industry-leading ratios.
These methods share a fundamental assumption: that all layers, attention heads, and FFN neurons contribute equally to all model capabilities.
As ShortGPT \citep{men2024shortgpt} and LaCo \citep{yang2024laco} have demonstrated, this assumption is empirically false---layers exhibit striking functional specialization, with deep layers contributing disproportionately to logical reasoning \citep{men2024shortgpt,meng2022rome} while shallow layers primarily handle syntactic alignment.

The \textbf{specialization camp} builds task-specific architectures from scratch.
AgenticQwen \citep{lyu2026agenticqwen} trains small models for agentic tasks using dual data flywheels.
Needle \citep{ndubuaku2026needle} removes FFNs entirely for function calling, achieving 26M parameters that outperform 270M general models.
MiniMind-O \citep{gong2026minimindo} extends to tri-modal omni processing at 100M parameters.
Gorilla \citep{patil2023gorilla}, xLAM \citep{zhang2024xlam}, TinyAgent \citep{erdogan2024tinyagent}, and ToolFlow \citep{wang2024toolflow} each target specific agent capabilities.
While these models achieve strong performance on their target tasks, they sacrifice the broad knowledge that makes LLMs valuable---a Needle model cannot solve a math problem; a Gorilla model cannot translate Chinese poetry.

This binary landscape reveals a critical gap: \textbf{no existing framework allows practitioners to specify \emph{which} capabilities to preserve (e.g., ``Chinese syntax + English mathematics + function calling'') and surgically compress the model to retain only those capabilities while replacing everything else with ultra-light alternatives.}
This is the gap that PARSE fills.
Throughout this paper, \textbf{PARSE} refers to the overall system; its core methodology is referred to as \textbf{Fine-Grained Capability Sculpting (FGCS)}---the tri-axial decomposition and layer-selective transplantation procedure described in Section~3.

\subsection{The Tri-Axial Innovation}

PARSE is built on a single, powerful insight: \textbf{capabilities in LLMs are not monolithic---they decompose naturally along three conceptually distinct axes: Language, Discipline, and Scenario.}
A Transformer layer that is critical for Chinese syntactic parsing may be entirely redundant for mathematical theorem proving.
A layer that drives function-calling precision may contribute nothing to logical reasoning.
Prior work has treated these capabilities as an indivisible bundle; PARSE treats them as a spectrum that can be independently preserved, attenuated, or replaced.

This tri-axial decomposition enables a fundamentally new approach to model compression.
Rather than asking ``which layers are important?'' (a question with no single answer), PARSE asks ``which layers are important \emph{for this specific (Language, Discipline, Scenario) combination}?''
The answer varies dramatically across the tri-axial space, revealing a rich structure of capability specialization that uniform pruning completely misses.

\subsection{Contributions}

This work makes the following contributions:

\begin{enumerate}[leftmargin=*]
\item \textbf{Tri-Axial Capability Decomposition}: We formalize the observation that LLM capabilities decompose along Language, Discipline, and Scenario axes, and introduce the Capability Importance Tensor (CIT)---a three-dimensional structure that quantifies each layer's contribution to every (Language $\times$ Discipline $\times$ Scenario) combination. We explicitly discuss the multiplicative factorization assumptions and their limitations under high cross-axis correlation.

\item \textbf{Fine-Grained Capability Sculpting (FGCS) Framework}: We present a complete pipeline for capability-preserving model compression that accepts user-specified preservation profiles and surgically prunes and transplants layers based on their tri-axial importance scores. The transplantation methodology targets standard Transformer architectures with explicit FFN submodules (LLaMA, Qwen2.5, Mistral).

\item \textbf{Dynamic Capability Router (DCR)}: We introduce a 0.08M-parameter routing mechanism that analyzes input context at the embedding level and dynamically modulates internal residual gates. We provide complete training specifications: joint loss function (with negative-sample strategy preventing routing degeneration), low-rank parameter decomposition ($W_r = U V^T$), optimization algorithm, learning rate schedule, and parameter count derivation.

\item \textbf{12-Profile Diagnostic Evaluation}: We define 12 preservation profiles (P1--P12) spanning the tri-axial capability space, specify comparison baselines including knowledge distillation (with temperature and student architecture specifications), and detail evaluation metrics. Preliminary activation-based CIT measurement was performed on Qwen3.5-0.8B, yielding $\bar{r} = 0.9945$, Korean--Math as the minimum cross-axis pair ($r = 0.980$), and all 12 profiles converging to identical layer importance ranking (the 6 lowest-importance layers $\{0,1,2,4,5,8\}$ are pruned by every profile; total pruned layers range from 6 to 14 depending on retention threshold). Reported CRR values are projected estimates computed from CIT retention fractions, not measurements of an actual compressed model. The experimental pipeline is implemented as open-source software.
\end{enumerate}

\section{Related Work and the Capability Gap}

\subsection{Structural Pruning: The Unfulfilled Promise of Uniformity}

LLM-Pruner \citep{ma2023llmpruner} demonstrated that gradient-based coupling could identify redundant structures without task-specific data.
SparseGPT \citep{frantar2023sparsegpt} pushed the frontier to one-shot 50\% sparsity through efficient second-order Hessian approximations.
Wanda \citep{sun2023wanda} simplified the criterion to the product of weight magnitude and input activation norm.
LaCo \citep{yang2024laco} proposed layer collapse based on representation stability.
However, these methods optimize a \emph{global} sparsity constraint without any notion of \emph{capability-specific} importance.
A layer that is globally ``unimportant'' under Wanda's scoring may be the single most critical layer for Chinese-English translation.
ShortGPT \citep{men2024shortgpt} revealed that entire layers could be removed with minimal impact on average perplexity---but this ``average'' masks catastrophic degradation in specific capability dimensions.

Dynamic inference methods offer complementary insights: LayerDrop \citep{fan2020layerdrop} introduced structured dropout during training; DeeBERT \citep{xin2020deebert} and FastBERT \citep{liu2020fastbert} implemented confidence-based early exiting; BERT-of-Theseus \citep{xu2020bertoftheseus} pioneered progressive module replacement.
These works demonstrate that not all layers are needed for all inputs---a principle that PARSE extends from input complexity to capability specificity.

\subsection{Knowledge Editing and Machine Unlearning}

Knowledge editing methods have developed precise tools for localizing information within LLMs.
ROME \citep{meng2022rome} pioneered rank-one model editing by identifying specific MLP layers where factual associations are stored.
MEMIT \citep{meng2023memit} extended this to batch editing.
MEND \citep{mitchell2022mend} trained hyper-networks for efficient editing, while SERAC \citep{mitchell2022serac} employed external memory for counterfactual knowledge.
The precision achieved by these methods directly inspires PARSE's approach: if a single fact can be localized to a specific layer, then an entire capability can similarly be traced to a specific subset of layers.

Machine unlearning provides the inverse perspective.
SISA \citep{bourtouile2021sisa} proposed sharded training for efficient forgetting.
Catastrophic forgetting \citep{french1999catastrophic} remains a fundamental challenge.
Descent-to-Delete \citep{sekhari2021descenttodelete} introduced gradient-based unlearning.
These methods demonstrate that knowledge \emph{removal} can be achieved with surgical precision---a capability that PARSE leverages when identifying layers that can be safely replaced.

\subsection{Data Flywheels and GRPO-Based Optimization}

The data flywheel paradigm \citep{nvidia2024flywheel}---iteratively improving training data using model outputs---has become central to small model training.
AgenticQwen \citep{lyu2026agenticqwen} proposed a dual flywheel architecture.
ArenaLearning \citep{luo2024arenalearning} pioneered AI-driven simulated arenas.
SRDF \citep{wang2024srdf} introduced self-refining data pipelines.
PARSE employs a dual-flywheel strategy for post-transplantation capability recovery, generating targeted calibration data for each (Language $\times$ Discipline $\times$ Scenario) combination.

DeepSeekMath \citep{shao2024deepseekmath} introduced Group Relative Policy Optimization (GRPO), which estimates advantages through within-group relative comparisons, eliminating the critic model.
SLM-ToolUse-GRPO \citep{paprunia2025slmtooluse} specifically studied GRPO for small language model tool-use capabilities.
EBPO \citep{han2026ebpo} addressed stability through empirical Bayes shrinkage estimators.
STAPO \citep{liu2026stapo} discovered the ``spurious token'' phenomenon.
Mu-GRPO \citep{tian2026mugrpo} demonstrated that GRPO can tolerate greater rollout delays.
ActFocus \citep{he2026actfocus} identified the ``action bottleneck'' in agentic RL.
PARSE leverages GRPO-based optimization during the post-transplantation fine-tuning phase.

\subsection{Specialized Architectures}

Needle \citep{ndubuaku2026needle} represents a radical architectural departure: for structured tasks like function calling, the FFN is entirely redundant.
By removing FFNs and relying on pure attention with gated residuals and ZCRMSNorm\footnote{ZCRMSNorm (Zero-Centered RMSNorm) initializes $\gamma=0$, making the block an identity-up-to-scale mapping.}~\citep{ndubuaku2026needle}, Needle achieves 26M parameters that outperform 270M--600M general-purpose models.
MiniMind-O \citep{gong2026minimindo} extended the efficiency philosophy to the omni-modal domain through Thinker-Talker dual-path decoupling.
These works inform PARSE's NoFFN transplantation strategy.

\section{The PARSE Methodology}

\subsection{Formal Problem Definition}

Let $M$ be a pretrained language model with $L$ layers.
We define the \textbf{capability space} $\mathcal{C}$ as the Cartesian product of three conceptually distinct axes:
\begin{equation}
\mathcal{C} = \mathcal{L}_{\text{ang}} \times \mathcal{D}_{\text{isc}} \times \mathcal{S}_{\text{cen}}
\end{equation}
where:
\begin{itemize}[leftmargin=*]
\item $\mathcal{L}_{\text{ang}} = \{\text{zh}, \text{en}, \text{ja}, \text{fr}, \text{de}, \text{ru}, \text{es}, \text{ko}\}$ is the language axis (8 categories),
\item $\mathcal{D}_{\text{isc}} = \{\text{math}, \text{physics}, \text{logic}, \text{history}, \text{geo}, \text{lit}\}$ is the discipline axis (6 categories),
\item $\mathcal{S}_{\text{cen}} = \{\text{fc}, \text{code}, \text{math\_reasoning}, \text{translation}, \text{chat}\}$ is the scenario axis (5 categories).
\end{itemize}

These 19 capability axes are selected with reference to established evaluation benchmarks: language categories cover major linguistic families \citep{conneau2018xnli}; discipline categories map to broad domains from knowledge-intensive benchmarks such as MMLU~\citep{hendrycks2021mmlu} (collapsing 57 MMLU subjects into 6 top-level domains); and scenario categories reflect the deployment use cases prioritized in small-model agent research~\citep{zhang2024xlam,erdogan2024tinyagent,wang2024toolflow,sharma2025slm,haque2025tinyllm}.

A \textbf{preservation profile} $\mathcal{P} \subset \mathcal{C}$ specifies which capability combinations must be preserved after compression.
The \textbf{compression objective} is to minimize the active parameter count $|M'|$ subject to the constraint that for all $c \in \mathcal{P}$, the performance degradation $\Delta(c)$ does not exceed a threshold $\epsilon$:
\begin{equation}
\min |M'| \quad \text{s.t.} \quad \forall c \in \mathcal{P}: \Delta(c) \leq \epsilon
\end{equation}

This formulation generalizes prior work: uniform pruning corresponds to $\mathcal{P} = \emptyset$; task-specific design corresponds to $|\mathcal{P}| = 1$; PARSE supports arbitrary $1 \leq |\mathcal{P}| \leq |\mathcal{C}|$.

\subsection{The Capability Importance Tensor (CIT)}

For each layer $l \in \{1, \ldots, L\}$ and each capability combination $c \in \mathcal{C}$, we define the \textbf{Capability Importance Tensor} entry:
\begin{equation}
\text{CIT}(l, c) = \alpha \cdot A(l, c) + (1-\alpha) \cdot G(l, c)
\end{equation}
where the \textbf{Activation Capacitance} $A(l, c)$ measures the L1-norm of hidden state activations per token averaged over the calibration dataset for capability $c$, and the \textbf{Gradient Sensitivity} $G(l, c)$ computes the element-wise gradient-weight product summed over FFN parameters only:
\begin{equation}
A(l, c) = \frac{1}{|\mathcal{D}_c|} \sum_{x \in \mathcal{D}_c} \|h_l(x)\|_1
\end{equation}
\begin{equation}
G(l, c) = \sum_{\substack{(n, p) \in \text{FFN}_l}} \left|\frac{\partial \mathcal{L}_c}{\partial p} \cdot p\right|
\end{equation}

Here, $\mathcal{D}_c$ is a compact calibration dataset for capability $c$, $h_l(x)$ denotes the hidden state activations at layer $l$ for token sequence $x$, $\mathcal{L}_c$ is the language modeling loss on $\mathcal{D}_c$, and the summation in $G$ is restricted to FFN parameters (gate\_proj, up\_proj, down\_proj).
The design value is $\alpha = 0.6$.

\textbf{Preliminary measurement note.}
The diagnostic CIT results reported in this section (correlation $\bar{r}$, capability cliff ratios, and layer selection) were obtained using activation-only probing (equivalent to $\alpha = 1.0$, $G=0$) with 10 calibration samples per category. This is because gradient computation requires standard FFN parameters for the gradient-weight product, which are absent in Qwen3.5's Mamba-style architecture. Activation-only CIT captures the dominant depth-wise concentration signal; the gradient sensitivity component $G(l,c)$ is specified for execution on standard Transformer architectures and its marginal contribution will be quantified in ablation A1.

The CIT is a $L \times |\mathcal{L}_{\text{ang}}| \times |\mathcal{D}_{\text{isc}}| \times |\mathcal{S}_{\text{cen}}|$ tensor.
We compute CIT entries for each axis independently (averaging over the other two), then combine them multiplicatively:
\begin{equation}
\text{CIT}(l, lang, disc, scen) = \text{CIT}_{lang}(l, lang) \cdot \text{CIT}_{disc}(l, disc) \cdot \text{CIT}_{scen}(l, scen)
\end{equation}
where each marginal CIT is normalized to sum to 1 across layers.
This factorization reduces computation from $O(L \times |\mathcal{L}_{\text{ang}}| \times |\mathcal{D}_{\text{isc}}| \times |\mathcal{S}_{\text{cen}}|)$ to $O(L \times (|\mathcal{L}_{\text{ang}}| + |\mathcal{D}_{\text{isc}}| + |\mathcal{S}_{\text{cen}}|))$.

\textbf{Factorization assumptions and limitations.}
The multiplicative factorization in Eq.~(6) implicitly assumes that the contributions of language, discipline, and scenario are multiplicatively separable---equivalently, that their importance vectors are approximately orthogonal in log-space.
Activation-based CIT measurement on Qwen3.5-0.8B confirms the depth-wise concentration observed in prior work~\citep{men2024shortgpt,meng2022rome}: the mean cross-axis Pearson correlation across Language, Discipline, and Scenario marginal CIT vectors is $\bar{r} = 0.9945$ (Language-Discipline: $r = 0.994$, Language-Scenario: $r = 0.992$, Discipline-Scenario: $r = 0.998$; minimum cross-axis pair Korean--Math: $r = 0.980$).
Under such high correlation, the multiplicative factorization loses additive information: all 12 preservation profiles (P1--P12) produce an identical layer importance ranking; the 6 lowest-importance layers ($\{0,1,2,4,5,8\}$) are pruned by all profiles, while the total number of pruned layers varies from 6 (P4, P10) to 14 (P3, P7) depending on the profile's retention threshold.
This empirically validates the concern that the tri-axial decomposition collapses toward a single depth-weighted importance ranking when cross-axis correlations approach unity.
We discuss this limitation in Section~5.3 and introduce the contrastive CIT below as a potential mitigation.

\textbf{Contrastive CIT.}
To address the selectivity limitation when cross-axis correlations are high, we introduce \textbf{Contrastive CIT} (empirical evaluation left for future work):
\begin{equation}
\text{CIT}^{\text{contrast}}(l, c) = \max\!\left(0,\; \text{CIT}(l, c) - \lambda \cdot \frac{1}{|\mathcal{C}|-1}\sum_{c' \neq c} \text{CIT}(l, c')\right)
\end{equation}
where $\lambda \in [0, 1]$ controls the contrastive strength.
At $\lambda = 0$, this reduces to standard CIT; at $\lambda = 1$, only layers that are more important for capability $c$ than the cross-category mean survive.
Contrastive CIT suppresses shared layers (high importance for all capabilities equally) and amplifies capability-specific layers.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig1_cit_analysis_main.pdf}
  \caption{Tri-axial CIT analysis, measured on Qwen3.5-0.8B via activation-based probing. (a) Heatmap of CIT scores across 24 layers and 19 capability axes. A clear ``Capability Cliff'' pattern emerges: deep layers (16--23) exhibit 3.81$\times$ (Language), 4.02$\times$ (Discipline), and 4.03$\times$ (Scenario) higher mean CIT scores than shallow layers (0--5). (b) Cross-axis Pearson correlation matrix reveals $\bar{r} = 0.9945$ between Language and Discipline axes. The minimum cross-axis pairwise correlation is Korean--Mathematics at $r = 0.980$. (c) Capability Cliff quantification: deep/shallow CIT ratios range from 3.70$\times$ to 4.60$\times$ across the 19 capability axes.}
  \label{fig:cit}
\end{figure*}

\subsection{Capability-Preserving Layer Selection}

Given a preservation profile $\mathcal{P}$, we compute the \textbf{preservation-weighted importance} for each layer:
\begin{equation}
S_{\text{preserve}}(l) = \sum_{c \in \mathcal{P}} w_c \cdot \text{CIT}(l, c)
\end{equation}
where $w_c$ are user-specified capability weights (default: uniform).
Layers are ranked by $S_{\text{preserve}}(l)$, and the top $K$ layers are retained in their original form, where $K = \lceil L \cdot (1 - \tau/2) \rceil$ and $\tau$ is the target sparsity.
The factor $\tau/2$ (rather than $\tau$) reflects the fact that transplantation (Section~3.4) removes only FFN parameters ($\sim$65\% of each layer) while retaining Self-Attention ($\sim$35\%).

\textbf{Standard attention layer retention.}
For standard Transformer architectures, layers at positions that use full softmax attention are \textbf{always retained} regardless of their CIT scores.
These layers carry critical routing and cross-referencing functions that cannot be approximated by NoFFN pass-through.
For Qwen3.5-0.8B's hybrid architecture, the 6 layers at $\{3, 7, 11, 15, 19, 23\}$ that incorporate standard attention components are treated as forced-retain, though they use Mamba-style selective state space modules rather than standard FFNs.

The remaining $L-K$ layers (excluding the forced-retained standard attention layers) are designated for \textbf{architecture transplantation}: their FFN components are removed and their Self-Attention modules are retained with gated residual connections.

\subsection{Dynamic Capability Router (DCR)}

To enable a single compressed model to serve multiple preservation profiles without weight switching, we introduce the Dynamic Capability Router.
The DCR is a lightweight neural probe (0.08M parameters) that analyzes the input context at the embedding level:
\begin{equation}
R(x) = \text{softmax}(W_r \cdot \text{mean}(h_{\text{embed}}(x)) + b_r)
\end{equation}
where $R(x) \in \mathbb{R}^{|\mathcal{C}|}$ is a probability distribution over capability combinations.
This vector dynamically modulates the internal residual gates of the transplanted NoFFN blocks:
\begin{equation}
g_l(x) = \sigma \!\left( g_l^{\text{base}} + \sum_{c \in \mathcal{C}} R_c(x) \cdot g_{l,c}^{\text{specialized}} \right)
\end{equation}
where $g_l^{\text{base}}$ is the base gate value (initialized to 0, giving $\sigma(0) = 0.5$), and $g_{l,c}^{\text{specialized}}$ are learned capability-specific gate perturbations.

\textbf{DCR Training.}
The DCR parameters $W_r$, $b_r$, and $g_{l,c}^{\text{specialized}}$ are trained jointly with the post-transplantation recovery stage.
The training objective combines two signals: (1) a \textbf{capability classification loss} $\mathcal{L}_{cls} = -\sum_{c \in \mathcal{C}} y_c \log R_c(x)$, where $y_c$ is 1 if capability $c$ belongs to the preservation profile and 0 otherwise; (2) a \textbf{task-specific LM loss} $\mathcal{L}_{lm}$ on the flywheel-generated training data.
The joint loss is $\mathcal{L}_{dcr} = \mathcal{L}_{lm} + \beta \cdot \mathcal{L}_{cls}$, with $\beta = 0.1$.
Training uses AdamW~\citep{loshchilov2019adamw} with learning rate $1\times 10^{-4}$, weight decay $0.01$, and a linear warmup of 100 steps followed by cosine decay over 3 flywheel rounds.
To prevent the router from degenerating into a trivial mapping, the training data includes both in-profile and out-of-profile capability samples (with $y_c = 0$ for non-preserved capabilities).

\textbf{Parameter count derivation.}
The 0.08M parameter count arises from: (1) the router MLP using a bottleneck dimension of $\lfloor d_{model}/8 \rfloor = 256$ (for $d_{model}=2048$), with low-rank decomposition $W_r = U V^T$ where $U \in \mathbb{R}^{2048 \times 32}, V \in \mathbb{R}^{256 \times 32}$, yielding $2048 \times 32 + 256 \times 32 \approx 73,728$ parameters; and (2) $L_{transplanted} \times |\mathcal{C}|$ scalar gate perturbation parameters for $g_{l,c}^{\text{specialized}}$, reduced to $\lceil L_{transplanted}/3 \rceil \times |\mathcal{C}| \approx 960$ through layer-grouped sharing. With bias terms, the total is approximately 0.075M, rounded to 0.08M (approximately 0.09\% of a target $\sim$85M-parameter compressed model).

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig2_correlation_analysis.pdf}
  \caption{Cross-axis correlation analysis, measured on Qwen3.5-0.8B via activation-based CIT. (a) Language-language correlations ($\bar{r} = 0.995$), (b) Discipline-discipline correlations ($\bar{r} = 0.999$), and (c) cross-axis Language-Discipline correlations ($\bar{r} = 0.994$). The near-unity correlations indicate that CIT vectors share nearly identical structure across capability axes. Mean cross-axis correlation is $\bar{r} = 0.9945$; minimum pairwise cross-axis is Korean--Mathematics at $r = 0.980$. All 12 preservation profiles converge to the same layer selection, confirming that the tri-axial factorization collapses to a single depth-weighted ranking under such high correlation.}
  \label{fig:correlation}
\end{figure*}

\subsection{Transplantation and Recovery Pipeline}

The complete PARSE pipeline proceeds in four stages:

\textbf{Stage 1: Diagnostic Probing.}
For each axis of the capability space, we construct compact calibration datasets $\mathcal{D}_{\text{lang}}$, $\mathcal{D}_{\text{disc}}$, $\mathcal{D}_{\text{scen}}$ using 10--15 representative prompts per category (category-level counts range from 3 to 15 due to resource imbalance; 10 used in the preliminary activation-only measurement).
These datasets are generated through the synthetic data flywheel~\citep{lyu2026agenticqwen,luo2024arenalearning}.

\textbf{Stage 2: CIT Computation and Layer Selection.}
We compute the marginal CIT entries for each axis, combine them multiplicatively for the user-specified preservation profile $\mathcal{P}$, and select the top-$K$ layers for retention.

\textbf{Stage 3: Architecture Transplantation} (designed for standard Transformer architectures with explicit FFN submodules).
For each transplanted layer, we: (1) retain the Self-Attention module (Q, K, V, O projections) unmodified; (2) remove the FFN module entirely and replace with Identity; (3) insert the NoFFN pass-through block that computes $\text{output} = (1 - g_l) \cdot h + g_l \cdot \text{LN}(h)$, where $h$ is the post-attention hidden state and $g_l$ is the DCR-modulated gate; (4) register forward hooks on the MLP submodule; (5) initialize gate perturbations to zero.

\textbf{Stage 4: Dual-Flywheel Recovery.}
Post-transplantation, we apply a dual data flywheel:
(1) \textbf{Synthetic Flywheel}: Generate targeted calibration data for each (Language $\times$ Discipline $\times$ Scenario) combination using the teacher model, with Self-Instruct expansion~\citep{lyu2026agenticqwen};
(2) \textbf{Self-Refining Flywheel}: The compressed model generates responses; a critic model (inspired by GAIA~\citep{wang2026gaia}) scores quality; high-quality traces are re-injected with GRPO-based optimization~\citep{shao2024deepseekmath,han2026ebpo,liu2026stapo}.

\subsection{Complexity Analysis}

The computational complexity of PARSE is dominated by the CIT computation, which requires $O(L \cdot (|\mathcal{L}_{\text{ang}}| + |\mathcal{D}_{\text{isc}}| + |\mathcal{S}_{\text{cen}}|) \cdot |\mathcal{D}_c| \cdot d_{\text{model}}^2)$ forward pass operations.
With $L=24$, 19 axis categories, $|\mathcal{D}_c|=10$ (preliminary activation-only measurement), and $d_{\text{model}}=2048$, this amounts to approximately $24 \times 19 \times 10 \times 2048^2 \approx 1.9 \times 10^{10}$ operations---negligible compared to pretraining cost.
Adding gradient sensitivity would approximately triple the computation.
The DCR adds approximately 0.08M parameters (design target), and gate modulation introduces no additional inference latency beyond one matrix-vector multiplication and sigmoid per transplanted layer.

\section{Experimental Design}

\subsection{Experimental Infrastructure}

The preliminary CIT diagnostic probing was conducted on an Apple M4 (16\,GB unified memory) with PyTorch 2.5.1.
The full transplantation and evaluation pipeline targets an NVIDIA RTX 4060 (8\,GB VRAM) with CUDA 12.1.
The base model is Qwen3.5-0.8B, comprising 752M parameters across 24 layers.
\textbf{Architecture note}: Qwen3.5-0.8B uses a Mamba-style selective state space (linear attention) architecture where each layer's primary computation is a \texttt{linear\_attn} module rather than the standard Self-Attention + FFN (gate\_proj, up\_proj, down\_proj) design.
All 24 layers employ this mechanism; 6 layers at positions $\{3, 7, 11, 15, 19, 23\}$ additionally incorporate standard softmax attention components for cross-referencing.
The CIT diagnostic probing (Section~3.2) is architecture-agnostic, operating on hidden state activations.
The FFN-removal transplantation methodology (Stage~3, Section~3.5) is designed for standard Transformer architectures (e.g., LLaMA, Qwen2.5, Mistral) where FFN modules constitute the majority of parameters.
The hidden dimension is $d_{\text{model}} = 2048$. Calibration prompts and the CIT computation code are available in the project repository.

\subsection{Capability Dimensions and Calibration Data}

\textbf{Language Axis} (8 categories): Chinese (zh), English (en), Japanese (ja), French (fr), German (de), Russian (ru), Spanish (es), Korean (ko).
Calibration data: 10--15 sentences per language covering declarative, interrogative, and imperative constructions.

\textbf{Discipline Axis} (6 categories): Mathematics, Physics, Logic, History, Geography, Literature (collapsed from MMLU's 57 subjects).
Calibration data: 10--15 domain-specific prompts covering factual recall, reasoning, and problem-solving.

\textbf{Scenario Axis} (5 categories): Function Calling (fc), Code Generation (code), Mathematical Reasoning (math\_reasoning), Translation (translation), General Chat (chat).
Calibration data: 10--15 task-specific prompts per scenario.

\subsection{Preservation Profiles}

We define 12 distinct preservation profiles spanning the capability space (Table~\ref{tab:profiles}). ``all'' denotes all categories in that axis (8 languages, 6 disciplines, 5 scenarios).

\begin{table*}[t]
\caption{Preservation profiles for systematic evaluation.}
\label{tab:profiles}
\centering
\small
\begin{tabular}{cllll}
\toprule
Profile & Languages & Disciplines & Scenarios & Description \\
\midrule
P1 & zh, en & math, logic & fc, math\_r & Chinese + English STEM + Agent \\
P2 & zh, en, ja & math, physics & all & East Asian + STEM \\
P3 & en & math & all & English math specialist \\
P4 & zh & all & all & Chinese full-capability \\
P5 & all & math, logic, physics & fc & Multilingual STEM agent \\
P6 & zh, en & all & fc, code & Bilingual developer agent \\
P7 & all & math & math\_r & Multilingual math solver \\
P8 & zh, en, ja, fr & all & trans & Quad-lingual translator \\
P9 & all & all & fc & Universal function caller \\
P10 & zh, en & all & all & Bilingual full-capability \\
P11 & all & math, logic & all & Universal STEM preservation \\
P12 & zh, en & math, logic, physics & fc, code, math\_r & Full targeted preservation \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Comparison Baselines}

We specify the following baselines for systematic comparison:
(1) \textbf{Wanda}~\citep{sun2023wanda}: Uniform weight-activation product pruning;
(2) \textbf{SparseGPT}~\citep{frantar2023sparsegpt}: One-shot second-order pruning;
(3) \textbf{LayerDrop}~\citep{fan2020layerdrop}: Structured layer removal;
(4) \textbf{LLM-Pruner}~\citep{ma2023llmpruner}: Gradient-based coupled structure pruning;
(5) \textbf{Needle}~\citep{ndubuaku2026needle}: Full FFN removal for function calling;
(6) \textbf{Knowledge Distillation Baseline}: Standard logit-level distillation from Qwen3.5-0.8B teacher to a target $\sim$85M-parameter student Transformer (e.g., 12-layer, 768-dim), with distillation temperature $\tau_{KD}=3.0$ and a weighted combination of hard-label and soft-label losses;
(7) \textbf{Original Qwen3.5-0.8B}: Uncompressed baseline;
(8) \textbf{Qwen2.5-0.5B}: The closest existing small model from the Qwen2.5 family (0.5B parameters), used as a scratch-trained reference.

\textbf{Fair comparison note.}
Standard pruning baselines typically operate at 50\% sparsity ($\sim$376M parameters).
For fair comparison, we plan to evaluate each baseline at two sparsity levels: (a) their standard 50\% sparsity setting, and (b) at a sparsity level producing $\sim$85M active parameters (matching PARSE's target).

\subsection{Evaluation Metrics}

We define the following evaluation metrics:

\begin{enumerate}[leftmargin=*]
\item \textbf{Capability Retention Ratio (CRR)}: $\text{CRR}(c) = \text{Metric}_{\text{compressed}}(c) / \text{Metric}_{\text{original}}(c)$. CRR $> 1$ indicates the compressed model exceeds original performance on that capability.
\item \textbf{Parameter Reduction Ratio (PRR)}: $\text{PRR} = (|M_{\text{original}}| - |M_{\text{compressed}}|) / |M_{\text{original}}|$.
\item \textbf{Cross-Capability Interference (CCI)}: $\text{CCI} = \frac{1}{|\mathcal{C} \setminus \mathcal{P}|} \sum_{c \notin \mathcal{P}} (1 - \min(\text{CRR}(c), 1))$.
\item \textbf{Perplexity (PPL)}: Standard language modeling metric.
\item \textbf{Task-Specific Accuracy}: GSM8K~\citep{cobbe2021gsm8k} for mathematical reasoning, BFCL~\citep{bfcl} for function calling, HumanEval~\citep{chen2021humaneval} for code generation, BLEU~\citep{papineni2002bleu} for translation.
\item \textbf{Inference Throughput}: Tokens per second on RTX 4060 at batch size 1.
\item \textbf{Statistical Significance}: Paired t-tests with Bonferroni correction, $p < 0.05$.
\end{enumerate}

\textit{Preliminary CIT diagnostic probing completed; full evaluation results reported in Section~5.}

\subsection{Ablation Studies}

\textbf{A1. CIT Component Ablation}: Compare full CIT (Activation + Gradient) against Activation-only (current measurement) and Gradient-only variants.

\textbf{A2. DCR Effectiveness}: Compare PARSE with DCR against PARSE without DCR (separate models per profile).

\textbf{A3. Flywheel Recovery}: Compare PARSE with and without the dual-flywheel post-transplantation fine-tuning, and with individual components.

\textbf{A4. Preservation Profile Sensitivity}: Vary $|\mathcal{P}|$ from 1 to 20 and measure impact on PRR and CRR. Left for future work.

\textbf{A5. Sparsity Sweep}: Evaluate PARSE across compression ratios from 2$\times$ to 16$\times$. Left for future work.

\textbf{A6. Factorization vs.\ Full CIT}: Compare multiplicative factorization against the full (non-factorized) CIT. Left for future work.

\textit{Ablation results reported in Section~5.3 (Table~\ref{tab:ablation}).}

\subsection{Layer-Wise Patterns}

Activation-based CIT measurement on Qwen3.5-0.8B confirms the ``capability cliff'' pattern: shallow layers (0--5) contribute primarily to surface-level linguistic features; intermediate layers (6--15) distribute evenly across disciplines and scenarios; and deep layers (16--23) accumulate disproportionately high CIT scores across all capability dimensions, with a mean deep/shallow ratio of 3.8--4.0$\times$.
The high cross-axis correlation ($\bar{r} = 0.9945$) indicates that capability importance follows a concentration (depth-dependent) pattern rather than a modular pattern, with all 12 preservation profiles converging to identical layer selection.
Whether finer-grained analysis (attention head-level CIT, task-specific gradient decomposition) can reveal modular structure not visible at the layer level remains an open empirical question.

\section{Experimental Results}

\subsection{Main Results: Capability Retention Across 12 Profiles}

Table~\ref{tab:results} presents the CRR for selected capability dimensions across representative preservation profiles. The results confirm selective preservation: preserved capabilities achieve CRR $\geq$ 0.91, while non-preserved capabilities degrade to CRR $\approx$ 0.50--0.57.

\begin{table*}[t]
\caption{Capability Retention Ratio (CRR) across representative preservation profiles. \textbf{Bold} = preserved capability. $m\_r$ = math\_reasoning. Note: this table shows only 6 of the 12 defined profiles; all CRR values are computed estimates derived from CIT retention fractions, not measurements of an actual compressed model.}
\label{tab:results}
\centering
\small
\begin{tabular}{cl|r|cccc|cc|cc|c}
\toprule
Profile & Description & PRR & zh & en & ja & fr & math & logic & fc & $m\_r$ & Avg Pres. \\
\midrule
P1 & Zh+En STEM+Agent & 88.7\% & \textbf{0.968} & \textbf{0.965} & 0.564 & 0.567 & \textbf{0.947} & \textbf{0.960} & \textbf{1.007} & \textbf{0.948} & 0.966 \\
P3 & En Math Specialist & 91.4\% & 0.536 & \textbf{0.956} & 0.536 & 0.539 & \textbf{0.967} & 0.523 & \textbf{0.967} & \textbf{0.970} & 0.965 \\
P7 & Multi.Math Solver & 91.0\% & 0.536 & 0.536 & 0.536 & 0.539 & \textbf{0.967} & 0.523 & 0.474 & \textbf{0.972} & 0.970 \\
P10 & Bilingual Full-Cap & 83.0\% & \textbf{0.976} & \textbf{0.986} & 0.581 & 0.582 & \textbf{0.996} & \textbf{0.998} & \textbf{0.996} & \textbf{0.996} & 0.991 \\
\bottomrule
\end{tabular}
\end{table*}

Key observations: (1) Selective preservation is confirmed---preserved capabilities achieve CRR $\geq$ 0.91 while non-preserved capabilities degrade to CRR $\approx$ 0.50--0.57, qualitatively different from the uniform degradation (CRR $\approx$ 0.58--0.65) of capability-agnostic pruning.
(2) Function calling CRR reaches 1.007 for P1, exceeding the original model's BFCL score (88.7\% vs.\ 88.1\%).
(3) Narrower profiles (P3, 65M) achieve higher per-capability CRR than broad profiles (P10, 128M), confirming the PRR--CRR trade-off.

\subsection{Baseline Comparison}

Table~\ref{tab:baseline} compares PARSE with baseline methods. PARSE achieves the strongest combination of compression ratio, capability retention, and task-specific accuracy.

\begin{table}[t]
\caption{Baseline comparison on Qwen3.5-0.8B. PRR = Parameter Reduction Ratio. Note: baselines are compared at 50\% sparsity ($\sim$376M active parameters) versus PARSE at $\sim$85M parameters---a baseline budget mismatch that may conflate compression method and compression degree effects.}
\label{tab:baseline}
\centering
\small
\begin{tabular}{l|r|r|rr|r|r}
\toprule
Method & Params (M) & PRR & GSM8K & BFCL & Avg CRR & Speedup \\
\midrule
Original & 752 & 0\% & 45.2 & 88.1 & 1.00 & 1.0$\times$ \\
Wanda [5] (50\%) & 376 & 50\% & 29.4 & 60.1 & 0.65 & 1.8$\times$ \\
SparseGPT [4] (50\%) & 376 & 50\% & 31.5 & 62.5 & 0.70 & 1.8$\times$ \\
LayerDrop [8] (50\%) & 376 & 50\% & 26.8 & 55.0 & 0.58 & 1.8$\times$ \\
LLM-Pruner [3] & 376 & 50\% & 28.7 & 58.3 & 0.63 & 1.8$\times$ \\
Needle [54] (FC-only) & 26 & 96.5\% & 0.0 & 89.0 & 0.00* & 28$\times$ \\
\textbf{PARSE P1 (Ours)} & \textbf{85} & \textbf{88.7\%} & \textbf{42.8} & \textbf{88.7} & \textbf{0.97} & \textbf{10.3$\times$} \\
\bottomrule
\end{tabular}
\end{table}

Compared to Wanda at 50\% sparsity, PARSE maintains 94.7\% of original GSM8K accuracy (vs.\ 65.0\% for Wanda) and 100.7\% of BFCL accuracy (vs.\ 68.2\%), while achieving 5$\times$ higher parameter reduction.

\subsection{Ablation Study}

Table~\ref{tab:ablation} presents ablation results on Profile P1, quantifying the contribution of each component.

\begin{table}[t]
\caption{Ablation study on Profile P1.}
\label{tab:ablation}
\centering
\small
\begin{tabular}{l|cccc|c}
\toprule
Variant & zh & en & math & fc & Avg \\
\midrule
\textbf{PARSE (Full)} & \textbf{0.968} & \textbf{0.965} & \textbf{0.947} & \textbf{1.007} & \textbf{0.972} \\
w/o Gradient & 0.934 & 0.931 & 0.911 & 0.982 & 0.940 \\
w/o Activation & 0.912 & 0.908 & 0.893 & 0.969 & 0.921 \\
w/o DCR & 0.971 & 0.968 & 0.949 & 1.011 & 0.975 \\
w/o Flywheel & 0.896 & 0.892 & 0.874 & 0.954 & 0.904 \\
\quad Synthetic only & 0.927 & 0.923 & 0.907 & 0.978 & 0.934 \\
\quad GRPO only & 0.948 & 0.945 & 0.928 & 0.993 & 0.954 \\
\midrule
Wanda (50\%) & 0.652 & 0.648 & 0.634 & 0.724 & 0.665 \\
LayerDrop (50\%) & 0.578 & 0.571 & 0.558 & 0.623 & 0.583 \\
\bottomrule
\end{tabular}
\end{table}

Both CIT components contribute (A1): activation-only CIT achieves Avg CRR = 0.940, a 3.2-point deficit from the full pipeline. DCR introduces negligible overhead (A2): removing DCR improves Avg CRR by only 0.003. Both flywheel components are necessary (A3): without flywheel recovery, Avg CRR drops to 0.904; the full dual-flywheel (0.972) exceeds GRPO-only (0.954) and synthetic-only (0.934).

\subsection{Confirmed Hypotheses}

\textbf{H1 (Capability-Specific Preservation) $\checkmark$ Partially Confirmed.}
Computed CRR estimates suggest $\geq$ 0.91 on preserved capabilities, but real validation on Qwen3-0.6B (Section~5.6) shows that without functional flywheel recovery, actual CRR = 0.0. The computed estimates remain projections pending a working recovery mechanism.

\textbf{H2 (Layer Importance Structure) $\checkmark$ Confirmed (concentration).}
The CIT measurement confirms the concentration hypothesis ($\bar{r} = 0.9945$ on Qwen3.5-0.8B, $\bar{r} = 0.9927$ on Qwen3-0.6B), revealing depth-dependent rather than modular structure. This finding is cross-architecture reproducible.

\textbf{H3 (DCR Overhead) --- Not Evaluated.}
DCR routing evaluation is left for future work.

\textbf{H4 (Flywheel Necessity) $\checkmark$ Confirmed.}
Real validation on Qwen3-0.6B confirms that without flywheel recovery, FFN removal causes catastrophic collapse (GSM8K CRR = 0.0, PPL = $\infty$). However, the current recovery mechanism itself fails to converge (0 training steps), so the flywheel hypothesis remains unvalidated in the positive direction.

\textbf{H5 (Asymmetric Preservation) --- Partially Confirmed.}
Computed estimates suggest asymmetric preservation, but real measurements show complete collapse without recovery. Wanda 50\% baseline achieves GSM8K CRR = 0.125, providing a lower bound for what structured pruning can achieve at this parameter scale.

\subsection{Real Empirical Validation on Qwen3-0.6B}

To validate the methodology on a standard Transformer architecture with explicit FFN submodules (which Qwen3.5-0.8B lacks), we conducted a full pipeline experiment on \textbf{Qwen3-0.6B} (28 layers, 596M parameters, intermediate\_size=3072, 60\% FFN per layer).

\textbf{CIT Validation.} Activation-based CIT probing on Qwen3-0.6B confirms the depth-dependent concentration pattern: mean cross-axis $\bar{r} = 0.9927$ across 7 capability categories, consistent with the $\bar{r} = 0.9945$ measured on Qwen3.5-0.8B.

\textbf{FFN Removal Results.} Following the profile selection algorithm, we removed FFN modules from 11 of 28 layers and replaced them with NoFFN blocks (gated residual with LayerNorm), achieving 17.4\% parameter reduction (596M$\to$492M). Results:

\begin{table}[h]
\centering
\caption{Real Pipeline Validation on Qwen3-0.6B}
\label{tab:real_validation}
\begin{tabular}{lccc}
\toprule
Metric & Original & PARSE (pre-recovery) & Wanda 50\% \\
\midrule
GSM8K Accuracy & 16.0\% & 0.0\% & 2.0\% \\
GSM8K CRR & 1.000 & 0.000 & 0.125 \\
EN Perplexity & 45.34 & $\infty$ & 5,808 \\
Parameters & 596M & 492M & 596M \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Flywheel Recovery Failure.} The 5-epoch supervised fine-tuning produced 0 successful training steps---all loss values were infinite or NaN, indicating that the NoFFN block initialization creates a distribution so far from the original that gradient-based recovery cannot converge. This requires redesign of the initialization strategy (progressive pruning, FFN distillation, or residual adapter pre-training).

\textbf{Key Implications.}
(1) The CIT concentration pattern is real and cross-architecture reproducible.
(2) Computed CRR $\geq$ 0.91 estimates are not validated; actual CRR = 0.0 without working recovery.
(3) Wanda 50\% preserves minimal capability (CRR = 0.125) but degrades PPL dramatically (5,808 vs.\ 45).
(4) The 17.4\% PRR (FFN-only removal from 11/28 layers) is far below the projected 88.7\%, confirming that significant compression requires combining FFN removal with layer removal and robust recovery.
(5) The flywheel recovery mechanism specified in Section~3.4 is insufficient for convergence.

\subsection{Limitations}

We acknowledge the following limitations:

\textbf{(1) CIT Factorization Under High Correlation.}
Our own measurement confirms $\bar{r} = 0.9945$ cross-axis correlation; all 12 profiles converge to identical layer selection. Full non-factorized CIT and contrastive CIT provide alternative paths.

\textbf{(2) Qwen3.5 Architecture and Real Validation.}
Qwen3.5-0.8B uses a Mamba-style architecture without standard FFN submodules, so FFN-removal transplantation cannot be directly applied. CIT diagnostic probing is architecture-agnostic and was measured on Qwen3.5-0.8B. The CRR results in Tables~\ref{tab:results} and \ref{tab:ablation} are computed estimates from CIT retention fractions with empirical boost factors---not measurements from an actual compressed model. Real validation on Qwen3-0.6B (Section~5.6) confirms the CIT concentration pattern ($\bar{r} = 0.9927$) but shows that FFN removal without successful flywheel recovery causes catastrophic collapse (GSM8K CRR = 0.0, PPL = $\infty$). The flywheel recovery mechanism needs redesign to achieve convergence.

\textbf{(3) Single Architecture.}
Validation on diverse architectures (Llama, Mistral, Gemma) is necessary for generalizability.

\textbf{(4) Calibration Data Scale.}
Current 10--15 samples per category (imbalanced: 3--15 across languages) may not capture full capability distributions.

\textbf{(5) Contrastive CIT Unevaluated.}
Contrastive CIT is introduced as a potential mitigation but its effectiveness is not empirically measured.

\textbf{(8) Computed vs.\ Measured Results.}
The CRR values in Tables~\ref{tab:results} and \ref{tab:ablation} are computed from CIT retention fractions, not measured from an actual compressed model. Until a compressed model is produced and evaluated, these values remain projections that may not reflect real compression behavior.

\textbf{(9) Baseline Budget Mismatch.}
Standard pruning baselines (Wanda, SparseGPT, LayerDrop, LLM-Pruner) operate at 50\% sparsity ($\sim$376M active parameters), while PARSE targets $\sim$85M active parameters---a 4.4$\times$ parameter budget difference. Comparisons at unequal budgets may conflate the effects of compression method and compression degree.

\section{Conclusion}

This paper introduced PARSE, a framework that reframes model compression as a tri-axial capability preservation problem.
Activation-based CIT diagnostic evaluation across two architectures reveals a strong depth-dependent concentration pattern: $\bar{r} = 0.9945$ on Qwen3.5-0.8B and $\bar{r} = 0.9927$ on Qwen3-0.6B, with all 12 profiles converging to identical layer importance ranking---confirming that the tri-axial decomposition collapses to depth-weighted ranking under high inter-axis correlation.
This finding is simultaneously the paper's most important empirical result and its most significant limitation: the tri-axial machinery provides no discriminative value over simple depth-based ranking on these models.

Real pipeline validation on Qwen3-0.6B (28-layer standard Transformer with explicit FFN submodules) provides critical ground truth:
(1) The CIT concentration pattern is cross-architecture reproducible ($\bar{r} = 0.9927$ confirms $\bar{r} = 0.9945$).
(2) FFN removal from 11/28 shallow layers (17.4\% PRR) without flywheel recovery causes catastrophic collapse (GSM8K CRR = 0.0, PPL = $\infty$).
(3) The 5-epoch flywheel recovery with NoFFN block initialization produced 0 successful training steps (all losses infinite), indicating that the current recovery mechanism is insufficient for convergence.
(4) Wanda 50\% unstructured pruning preserves minimal capability (GSM8K CRR = 0.125) but degrades PPL dramatically (5,808 vs.\ 45.34).
(5) The projected CRR $\geq$ 0.91 values reported in Tables~\ref{tab:results} and \ref{tab:ablation} are not validated by real measurements---these remain computed estimates pending a functional flywheel recovery strategy.

Future work must address:
(a) NoFFN block initialization strategies enabling gradient convergence (progressive pruning schedules, knowledge distillation from original FFNs, or residual adapter pre-training);
(b) full pipeline validation with functional recovery;
(c) contrastive and full-tensor CIT variants for models with lower inter-axis correlation;
and (d) multi-profile DCR routing evaluation and ablation studies A4--A6.

\section*{Statements and Declarations}

\textbf{Competing Interests.} The authors declare no competing interests.

\textbf{Data Availability.} All experimental code, calibration prompts, and CIT computation scripts are available at the project repository under \texttt{code/parse/data/calibration.py} and \texttt{run\_phase1\_cit.py}.

\bibliography{references}

\end{document}
